apiVersion: v1
kind: Service
metadata:
  name: {{ include "milkapp.fullname" . }}
  labels:
    {{- include "milkapp.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "milkapp.selectorLabels" . | nindent 4 }}
    app.kubernetes.io/component: app
